<UserControl x:Class="Classroom.Views.OktatoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Classroom.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="Oktató adatai" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Text="Vezetéknév:" FontWeight="Bold"/>
            <TextBox Text="{Binding Vnev}" Margin="0,0,0,10"/>
            <TextBlock Text="Keresztnév:" FontWeight="Bold"/>
            <TextBox Text="{Binding Knev}" Margin="0,0,0,10"/>
            <TextBlock Text="Kurzusok:" FontWeight="Bold"/>
            <ListBox ItemsSource="{Binding Kurzusok}" DisplayMemberPath="Nev" Margin="0,0,0,10"/>
        </StackPanel>
    </Grid>
</UserControl>
