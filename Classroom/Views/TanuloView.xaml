<UserControl x:Class="Classroom.Views.TanuloView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Classroom.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="Tanuló adatai" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Text="Név:" FontWeight="Bold"/>
            <TextBox Text="{Binding Nev}" Margin="0,0,0,10"/>
            <TextBlock Text="Email:" FontWeight="Bold"/>
            <TextBox Text="{Binding Email}" Margin="0,0,0,10"/>
            <TextBlock Text="Kurzus:" FontWeight="Bold"/>
            <ComboBox ItemsSource="{Binding Kurzus}" SelectedValue="{Binding KurzusId}" DisplayMemberPath="Nev" Margin="0,0,0,10"/>
            <Button x:Name="btSave" Content="Mentés"  Margin="0,10,0,0" Click="btSave_Click"/>
        </StackPanel>
    </Grid>

</UserControl>
